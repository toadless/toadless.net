// General imports

// Own imports
    // Variables
    import { colors } from "../_colors"

// Styles
const headerStyles = {
    socials: {
        color: "#ffffffff",
    },

    socialsIcon: {
        fontSize: "20px",
        cursor: "pointer",
        paddingRight: "8px",
    }
}

// Exports
export default headerStyles