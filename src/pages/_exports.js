// Export all pages under a single file, to allow a single import path under
// the Main component where pages are rendered
export { default as MainPage } from "./Main"
export { default as  ProjectsPage } from "./Projects"